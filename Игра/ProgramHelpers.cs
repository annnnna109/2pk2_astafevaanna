static class ProgramHelpers
{
    private static object key;
    static object Button
    {
        get
        {
            var but = new Console.RedKey() key;

            return but;
        }
    }
}